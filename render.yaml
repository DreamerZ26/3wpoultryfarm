services:
  - type: web
    name: 3wpoultryfarm
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn mysite.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: poultry-db
          property: connectionString
      - key: EMAIL_HOST_USER
        value: ""   # set your email for contact form
      - key: EMAIL_HOST_PASSWORD
        value: ""
      - key: DEFAULT_FROM_EMAIL
        value: "noreply@3wpoultry.com"
    autoDeploy: true

databases:
  - name: poultry-db
    plan: free